<h1 class="mainHeading">Sign Up</h1>
    <hr class="faded" />
    
<div class="formWrapper">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        
        <div class="errors">
            <%= devise_error_messages! %>
        </div>

        <div class="field">
            <%= f.label :email, class: :label %>
                <%= f.email_field :email, autofocus: true, required: true, class: :input %>
        </div>

        <div class="field">
            <%= f.label :email_confirmation, class: :label %>
                <%= f.email_field :email_confirmation, autocomplete: "off", required: true, class: :input %>
        </div>

        <div class="field">
            <%= f.label :mobile_number, class: :label %>
                <%= f.text_field :mobile_number, autocomplete: "off", required: true, class: :input %>
        </div>

        <div class="field">
            <%= f.label :mobile_number_confirmation, class: :label %>
                <%= f.text_field :mobile_number_confirmation, autocomplete: "off", required: true, class: :input %>
        </div>

        <div class="field">
            <%= f.label :password, class: :label %>
                <% if @minimum_password_length %>
                    <em>(<%= @minimum_password_length %> characters minimum)</em>
                <% end %>
                    <%= f.password_field :password, autocomplete: "off", required: true, class: :input %>
        </div>

        <div class="field">
            <%= f.label :password_confirmation, class: :label %>
                <%= f.password_field :password_confirmation, autocomplete: "off", required: true, class: :input %>
        </div>

        <div class="actions">
            <%= f.button "Sign Up", type: :submit, class: :button %>
        </div>
    <% end %>

    <%= render "devise/shared/links" %>
    <%= link_to "Back", :back, class: :formLink %>

</div>   
 