<h1 class="mainHeading">New Key</h1>
    <hr class="faded" />

    <p>Add, store and retrieve a key as many times as you wish for &pound;12 per year.</p>

<div class="formWrapper">
    <%= form_for @key do |f| %>

        <div class="field">
            <%= f.label "Choose a key name", class: :label %>
                <%= f.text_field :name, autofocus: true, required: true, class: :input %>
        </div>

        <div class="actions">
            <%= f.button "Add Key", type: :submit, class: :button %>
        </div>                                  

    <% end %>

    <%= link_to "Back", :back, class: :formLink %>
    
</div>

