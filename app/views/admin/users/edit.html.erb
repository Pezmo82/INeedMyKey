<h1 class="mainHeading">Edit - <%= @user.email %></h1>
    <hr class="faded" />

        <p>Give users administration privileges. The higher the number, the more power they have.</p>
            <ol>
                <li>End User - Has no controls over accounts (except self)</li>
                <li>Administrator - Has some powers (Robin)</li>
                <li>Super Administrator - Has super powers (Batman)</li>
            </ol>

<div class="formWrapper">
    <%= form_for [:admin, @user] do |f| %>

        <div class="field">
            <%= f.label :rank_id, class: :label %><br />
                <%= f.text_field :rank_id, required: true, class: :input %>
        </div>

        <div class="actions">
            <%= f.button "Submit changes to rank", type: :submit, method: :patch, class: :button %>
        </div>
    <% end %>
    
    <%= link_to "Back", :back, class: :formLink %>
</div>
