<h1 class="mainHeading">Edit - <%= @key.name %></h1>
    <hr class="faded" />

        <p>Change location of key</p>
            <ol>
                <li>Place current location table sorted by </li>
            </ol>

<div class="formWrapper">
    <%= form_for [:admin, @key] do |f| %>

        <div class="field">
            <%= f.label :location, class: :label %><br />
                <%= f.text_field :location, required: true, class: :input %>
        </div>

        <div class="actions">
            <%= f.button "Submit changes to key location", type: :submit, method: :patch, class: :button %>
        </div>
    <% end %>
    
    <%= link_to "Back", :back, class: :formLink %>
</div>
